# CI placeholder

Próximo paso (Fase 6): GitHub Actions con `game-ci/unity-builder` para Android AAB + tests EditMode.

Requisitos: Unity license secrets, cache de `Library/`, nunca subir keystores al repo.
